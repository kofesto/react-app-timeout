import React from 'react'

function Kofesto() {
    return (
        <div>
            <h2>hello this is a timeout function test logout component</h2>
            <p>This page only shows when the timer onIdle function is called during an idle state</p>
        </div>
    )
}

export default Kofesto
